/**
 * List of model types. Only final models should be listed here (no abstract).
 */
enum EModelTypes {
    RequestBody,
    TestSuite,
    TestCase,
    ApiRequestTestStep,
}

export default EModelTypes;
